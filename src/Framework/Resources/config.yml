services:
  memcached:
    class: Framework\Cache\MemcachedBuilder

  twig:
    class: Framework\View\TwigBuilder

  doctrine:
    class: Framework\Database\ORM\Doctrine

  router:
    class: Framework\Router\Router

  database.migrations:
    class: Framework\Database\Migrations

configuration:
  memcached:
    host: 127.0.0.1
    port: 11211

  twig:
    debug: true
    paths: /

  doctrine:
    driver: mysql_pdo
    user: default
    password: default
    dbname: default

  router:
    routing:
      framework_routing:

commands:
  database.migrations.migrate:
    class: Framework\Database\Command\MigrateCommand:migrate

  database.migrations.status:
    class: Framework\Database\Command\MigrateCommand:status

  database.migrations.generate:
    class: Framework\Database\Command\MigrateCommand:generate

  doctrine:
    class: Framework\Database\Command\MigrateCommand:schema